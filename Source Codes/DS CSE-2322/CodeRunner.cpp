#include <bits/stdc++.h>
using namespace std;

#define el "\n"
#define sz(a) (int) a.size()

int main()
{
      int k;
      string t, s;

      getline(cin, t);
      getline(cin, s);
      cin >> k;

      t.resize(sz(t) + sz(s));
      for (int i = sz(t) - 1; i > sz(s) - sz(t); i--)
      {
            t[]
      }

      cout << t << el << s << el;
}
